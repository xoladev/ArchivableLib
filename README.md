# ✨ ALib UI Library

> **ALib** — это современная, быстрая и эстетичная библиотека интерфейсов для Luau, разработанная с упором на плавность, эффекты размытия и простоту использования.

---

## 🚀 Особенности
* 🎨 **Modern Design** — Минималистичный стиль и поддержка тем (Dark Theme).
* 🌫️ **Visual Effects** — Встроенные функции для создания Blur-эффектов.
* ⚡ **Easy Integration** — Быстрый доступ через глобальную переменную `_G.ALib`.
* 🖱️ **User Experience** — Встроенная система Drag-and-Drop (перетаскивание) и уведомления.

---

## 🛠 Пример использования

```lua
-- 1. Создание основного меню (Название в Explorer, Заголовок, Темная тема)
local myMenu = _G.ALib:CreateMenu("NameInExplorer", "TitleInUI", true)

-- 2. Добавление вкладок (Объект меню, Название, ID Иконки)
_G.ALib:AddTab(myMenu, "Главная", _G.ALib.Assets.lolImage)
_G.ALib:AddTab(myMenu, "Настройки", _G.ALib.Assets.lolImage)

-- 3. Логика переключения интерфейса
local function toggleMenu()
    myMenu.Main.Visible = not myMenu.Main.Visible
end

-- 4. Создание плавающей кнопки (Название, ID Иконки, Функция)
_G.ALib:CreateFloatingButton("OpenBtn", _G.ALib.Assets.lolImage, toggleMenu)

-- 5. Отправка уведомления (Заголовок, Сообщение, ВремяДоУдаления, Картинка)
_G.ALib:CreateNotification("Система", "Библиотека успешно загружена!", 5, _G.ALib.Assets.lolImage)

-- 6. Отключение библиотеки
_G.ALib:DestroyLib()

-- 7. Сделать Blue эффект (Путь к обьекту к которому должен быть применён фильтр)
_G.ALib:MakeBlurEffect("game.Players.LocalPlayer.PlayerGui.Test.Frame")

-- 8. Сделать Draggable эффект (Путь к обьекту к которому должен быть применён фильтр)
_G.ALib:MakeDraggable("game.Players.LocalPlayer.PlayerGui.Test.Frame")
