# ✨ ALib UI Library

> **ALib** — это современная, быстрая и эстетичная библиотека интерфейсов для Luau, разработанная с упором на плавность и простоту использования.

---

## 🚀 Особенности
* 🎨 **Modern Design** — Минималистичный стиль, подходящий для любого проекта.
* ⚡ **Easy Integration** — Быстрый доступ ко всем функциям через глобальную переменную `_G.ALib`.
* 📦 **Built-in Assets** — Готовая библиотека иконок и ассетов внутри `_G.ALib.Assets`.
* 📱 **Floating Elements** — Поддержка плавающих кнопок и адаптивных табов.

---

## 🛠 Пример использования

Используйте этот шаблон для быстрого старта. Пример демонстрирует создание меню, добавление нескольких вкладок и создание плавающей кнопки переключения видимости.

```lua
-- [[ 1. Создание основного меню ]]
-- Аргументы: Заголовок, Подзаголовок, Включить ли тени/эффекты
local myMenu = _G.ALib:CreateMenu("lolCheat", "lolHub", true)

-- [[ 2. Добавление вкладок ]]
-- Аргументы: Объект меню, Название вкладки, Иконка из ассетов
_G.ALib:AddTab(myMenu, "TestTab", _G.ALib.Assets.lolImage)
_G.ALib:AddTab(myMenu, "1", _G.ALib.Assets.lolImage)
_G.ALib:AddTab(myMenu, "2", _G.ALib.Assets.lolImage)

-- [[ 3. Логика переключения интерфейса ]]
local function clickedbutton()
    if myMenu.Main.Visible == false then
        myMenu.Main.Visible = true
    else
        myMenu.Main.Visible = false
    end
end

-- [[ 4. Создание плавающей кнопки (FAB) ]]
-- Аргументы: Имя, Иконка, Функция при клике
_G.ALib:CreateFloatingButton("TestButton", _G.ALib.Assets.lolImage, clickedbutton)
